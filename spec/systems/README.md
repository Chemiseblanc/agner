# System Models

This layer is reserved for future models built on top of `genserver`, `supervisor`, or other higher-level abstractions.

Examples include service protocols, supervision trees with application-specific state, and composed subsystems.