# Core Failure Models

This axis of the core layer captures crash handling and exit propagation semantics that belong to the base actor runtime itself.

- `propagation/link_propagation` checks link and monitor notifications.
- `propagation/exception_propagation` checks error exit propagation.